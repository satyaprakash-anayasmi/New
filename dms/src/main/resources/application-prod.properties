# Production Properties - PTSI-style direct hardcoded connection
server.port=8080
server.address=0.0.0.0

# Database - direct connection to Render PostgreSQL (external host, always resolvable)
spring.datasource.url=jdbc:postgresql://dpg-d8f5di6q1p3s73diu8i0-a.ohio-postgres.render.com:5432/dms_db_prod_2026?sslmode=require
spring.datasource.username=dms_db_prod_2026_user
spring.datasource.password=AI7DHXPasTaANYR1X0EqkDy1GvkTTeL5adpg
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# File Storage
app.file.storage.location=./uploads

# JWT
app.jwt.secret=${JWT_SECRET:dms-super-secret-jwt-key-2026-production-render}
app.jwt.expiration=3600000

# Security default passwords
app.default.admin.password=${ADMIN_PASSWORD:admin_Sec#2026}
app.default.reviewer.password=${REVIEWER_PASSWORD:reviewer_Sec#2026}
app.default.uploader.password=${UPLOADER_PASSWORD:uploader_Sec#2026}

# HikariCP
spring.datasource.hikari.connection-timeout=30000

# Multipart
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
